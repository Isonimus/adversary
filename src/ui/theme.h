/**
 * @file theme.h
 * @brief Theme system - redirects to theme_manager.h
 * 
 * This file now redirects to the ThemeManager for runtime theme support.
 * All theme:: constants are now theme:: functions.
 */

#pragma once

// Redirect to theme_manager.h which contains the new runtime theme system
#include "theme_manager.h"
